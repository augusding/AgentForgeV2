{
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "powershell -Command \"$f='$CLAUDE_FILE'; if($f -match '\\.py$'){python -c \\\"import ast; ast.parse(open(r'$f').read()); print('✅ syntax ok: $f')\\\" 2>&1} elseif($f -match '\\.(tsx?|jsx?)$'){cd frontend; npx tsc --noEmit 2>&1 | Select-Object -First 5; cd ..}\""
      }
    ]
  }
}
